{
  "name": "repo-parser",
  "version": "1.0.0",
  "description": "recursively parse through a repo into a single file importable into any AI",
  "main": "createRepoSummary.js",
  "bin": {
    "cpr": "./createRepoSummary.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cli-progress": "^3.12.0",
    "ignore": "^5.3.1",
    "yargs": "^17.7.2"
  }
}
